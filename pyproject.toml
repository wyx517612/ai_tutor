[project]
name = "rag"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "wyx517612", email = "3457268794@qq.com" }
]
requires-python = ">=3.12"
dependencies = [
    "chromadb",
    "ipykernel>=7.3.0",
    "sentence-transformers>=5.7.0",
]

[project.scripts]
rag = "rag:main"

[build-system]
requires = ["uv_build>=0.12.1,<0.13.0"]
build-backend = "uv_build"
